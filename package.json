{
  "name": "agent-pm",
  "version": "1.0.0",
  "description": "PM Agent Dashboard",
  "scripts": {
    "build": "cd frontend && npm install && npm run build"
  },
  "engines": {
    "node": "18.x",
    "npm": "10.x"
  }
}